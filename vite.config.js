import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";

// https://vite.dev/config/
export default defineConfig({
  plugins: [react()],
  base: "/RBAC_UI/", // Set the base path to match your GitHub Pages repository name
});
